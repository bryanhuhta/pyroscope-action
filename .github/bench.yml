name: flamegraph.com
on:
  pull_request:
  workflow_dispatch:

jobs:
  benchmark-cpu:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.22.3
      - name: Install pprof
        run: go get -u github.com/google/pprof
      - name: Benchmark CPU
        run: go test -bench . -benchmem -benchtime 10s -cpuprofile cpu.out -o bench.exe ./...
      - name: Generate pprof
        run: pprof -proto bench.exe cpu.out
      - name: Upload to flamegraph.com
        run: cat profile001.pb.gz | curl --data-binary @- https://flamegraph.com
